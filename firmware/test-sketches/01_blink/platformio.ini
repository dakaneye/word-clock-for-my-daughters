; Blink-only sketch for breadboard Step 1 (ESP32 alone).
; Keeps the dependency set empty so we're only testing the power + flashing path.

[env:esp32]
platform = espressif32@6.8.0
framework = arduino
board = esp32dev
monitor_speed = 115200
build_flags = -std=gnu++17
build_unflags = -std=gnu++11
