; MicroSD listing for breadboard Step 3 (HW-125 breakout on VSPI).
; Mounts the card and walks the root directory.

[env:esp32]
platform = espressif32@6.8.0
framework = arduino
board = esp32dev
monitor_speed = 115200
build_flags = -std=gnu++17
build_unflags = -std=gnu++11
